# Configuration for build runners. See https://github.com/dart-lang/build/blob/master/build_config/README.md

targets:
  $default:
    sources:
      - assets/**
      - lib/$lib$
      - pubspec.*
      - $package$

builders:
  nitrogen_flutter_svg:
    import: "package:nitrogen_flutter_svg/nitrogen_flutter_svg.dart"
    builder_factories: [ "nitrogenFlutterSvgBuilder" ]
    build_extensions: { "$package$": [ "lib/src/svg_extension.nitrogen.dart" ] }
    auto_apply: dependents
    build_to: source
